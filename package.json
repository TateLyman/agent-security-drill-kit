{
  "name": "agent-security-drill-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "npm run check",
    "check": "node --check agent-security-drill.js",
    "test": "npm run check",
    "serve": "npx serve ."
  }
}
